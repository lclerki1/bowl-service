<%= form_for(@game_set) do |f| %>
  <% if @game_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_set.errors.count, "error") %> prohibited this game_set from being saved:</h2>

      <ul>
      <% @game_set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
